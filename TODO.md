# TODO

- [ ] Migrate the eight legacy suites under `src/{board,headquarters,reports,review,services,simulation,tutorial}/tests.rs` and `src/ui/headquarters/scene/tests.rs` into `tests/` as a separate change. Add `src/lib.rs`, make `main.rs` consume it, and remove test modules/helpers from `src/`. Preserve regression coverage through intentional public APIs; consolidate related board (seven cases) and simulation (six cases) inputs toward five cases per feature, or explain why distinct coverage warrants more (§11).
- [ ] Split the 713-line `src/game.rs` into cohesive action-handling and capture-setup modules. Reduce `Game::update` and `Game::begin_capture_scene` below 100 lines; keep each Rust file below 800 lines and use named module files (§2.2–2.3, §4.1).
- [ ] Extract cohesive helpers from oversized UI functions under `src/ui/headquarters`: `draw`, `chrome::hud`, `management::career`, `returns::detail`, `quest::draw`, `phone::planning`, and `scene::draw`. Bring each below 100 lines while preserving `UiAction` returns (§4.1, §7).
- [ ] Move pointer gesture tracking, drag suppression, and pending-action assignment out of `Game::draw` into explicit input/update handling. Keep rendering separate from state mutation and preserve release activation and drag cancellation (§5.1, §7.5).
- [ ] Move authored roster data, starting resources, promotion/review thresholds, service costs, recovery/training/scouting values, and caps from `simulation.rs`, `services.rs`, and `review.rs` into typed JSON under `assets/`. Load through toolkit APIs, validate game invariants, and add focused valid/invalid configuration cases in `tests/` (§5.3, §11).
- [ ] Move player-facing labels, tutorial/help text, report templates, and error copy into JSON. Derive displayed balance values from loaded configuration; align prompts with exact visible controls on phones and short screens, including the conditional Returns shortcut versus Departure room navigation (§5.3, §7.5).
- [ ] Load `assets/data/texture_manifest.json` through a typed toolkit loader in `Game::new`, replacing duplicated asset paths. Include chroma-key settings in the schema, validate required/unique keys and asset references, and report failing source/path context. Update `tests/asset_registry.rs` to load game-data JSON through toolkit APIs instead of direct `serde_json::from_str` (§5.3, §6, §8.3).
- [ ] Handle preference-load errors explicitly in `Game::new`: retain defaults for missing preferences, but surface corrupt/unreadable saves instead of silently using `unwrap_or_default()` (§6.2).
- [ ] Remove the write-only `Game::choosing_party` field and its assignments; rename used `_dt` to `dt`, remove unused `_compact` and `_g` parameters and their arguments, and rename the normalized capture-scene binding to avoid shadowing `scene` (§1.4, §10.3).
- [ ] Replace `Guild::objective_counts`' tuple return with a named result and update its consumers so certification and commission counts remain explicit (§4.4).
- [ ] Add `//!` purpose comments to `src/ui/help.rs`, `src/ui/headquarters/{chrome,management,planning,returns,scene}.rs`, and existing integration tests; document migrated test modules as part of their migration (§9.2).
- [ ] Complete a repeatable published-browser pointer/touch-control pass for new-guild confirmation, all tutorial steps, day-30 review, sandbox continuation, and restart at phone and short-landscape sizes, including larger text. Fix any inaccessible controls or mismatched prompts; record current evidence directly in `docs/verification/` (§7.5, §12).
- [ ] Remove superseded same-screen screenshots retained as historical evidence (`browser_*`, `phase2_*`, and duplicate legacy `ui_mobile_*` captures) from `docs/verification/`, retaining distinct current viewport/state evidence and updating document links. Replace same-state captures in place (§12).
